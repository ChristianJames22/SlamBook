<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/slam"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SlamBook"
        tools:targetApi="31">

        <!-- Splash Activity as the Launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity (opened after Splash) -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- Other Activities -->
        <activity android:name=".DrinkActivity" android:exported="false" />
        <activity android:name=".FoodActivity" android:exported="false" />
        <activity android:name=".PlaceActivity" android:exported="false" />
        <activity android:name=".BookActivity" android:exported="false" />
        <activity android:name=".SongsActivity" android:exported="false" />
        <activity android:name=".MovieActivity" android:exported="false" />
        <activity android:name=".UserActivity" android:exported="false" />

        <!-- Fragments or additional activities -->
        <activity android:name=".Me" android:exported="false"
            tools:ignore="Instantiatable" />
        <activity android:name=".ToDo" android:exported="false"
            tools:ignore="Instantiatable" />
        <activity android:name=".Home" android:exported="false"
            tools:ignore="Instantiatable" />
        <activity android:name=".AboutMe" android:exported="false" />

    </application>

</manifest>
